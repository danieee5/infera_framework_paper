# requirements.txt — pinned versions for reproducibility
# Install: pip install -r requirements.txt
# Tested on: CUDA 12.1, Python 3.10, RTX 4090

# Core inference framework
vllm==0.4.3

# PyTorch — must match vLLM 0.4.3 requirements
torch==2.3.0
torchvision==0.18.0

# Model loading and quantization
transformers==4.43.0
bitsandbytes==0.43.1    # INT8 W8A16 quantization
autoawq==0.2.5          # INT4 AWQ quantization
accelerate==0.31.0

# GPU measurement (NVML Python bindings)
pynvml==11.5.0

# Energy and CO2 tracking (triangulation source)
codecarbon==2.4.1

# Model download
huggingface_hub==0.24.0

# Tokenization fallback
tiktoken==0.7.0

# HTTP clients
requests==2.32.3    # used by generate_reproducibility_info.py
httpx==0.27.0       # async HTTP for concurrent vLLM requests (benchmark_runner.py)

# Utilities
numpy==1.26.4
